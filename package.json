{
  "name": "@busybusy/data-sync-web-worker",
  "version": "0.0.1",
  "description": "indexedDB data sync through web workers",
  "main": "data-sync.js",
  "scripts": {
    "deploy": "bin/deploy.sh",
    "build": "gulp",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "npm run build"
  },
  "author": "busy inc.",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^8.0.47",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0",
    "browserify": "^14.5.0",
    "dexie": "^2.0.1",
    "gulp": "^3.9.1",
    "vinyl-source-stream": "^1.1.0"
  }
}
